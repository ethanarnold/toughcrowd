// Add API handlers here as needed
// Example:
// import { http, HttpResponse } from 'msw'
// http.post('/api/generate-question', () => {
//   return HttpResponse.json({ question: 'Test question' })
// }),

export const handlers = []
